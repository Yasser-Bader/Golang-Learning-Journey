# 🏗️ Structs (Employee Data)

## 📝 وصف المهمة

إنشاء نوع بيانات مخصص (Custom Type) باستخدام الـ Struct لنمذجة بيانات موظف حقيقي (الاسم، السن، الراتب، الحالة)، مما يمهد لمفهوم الكائنات (Objects).

## 💡 مفاهيم تم تطبيقها

- **Defining Struct:** تجميع بيانات مختلفة النوع في مكان واحد.
- **Instance:** إنشاء نسخة من الموظف وتعبئة البيانات.
- **Access Fields:** الوصول للبيانات وتعديلها باستخدام النقطة (`.`).

## 🚀 طريقة التشغيل

```bash
go run main.go
```
