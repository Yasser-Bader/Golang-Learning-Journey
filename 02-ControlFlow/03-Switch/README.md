# 🔀 Switch Statement (Grade Calculator)

## 📝 وصف المهمة

برنامج تفاعلي بسيط لحساب تقدير الطالب (A, B, C...) بناءً على الدرجة المدخلة، باستخدام جملة `switch` التي توفر شكلاً أنظف من `if-else` المتعددة.

## 💡 مفاهيم تم تطبيقها

- **Interactive Input:** استخدام `fmt.Scanln`.
- **Switch Cases:** التعامل مع الشروط المتعددة (Cases) بدون الحاجة لـ `break`.
- **Clean Logic:** ترتيب الشروط من الأعلى للأسفل.

## 🚀 طريقة التشغيل

```bash
go run main.go
```
